# Nhật ký quyết định đồ án US Accidents

| Ngày | Quyết định | Bằng chứng đã xem | Phương án không chọn | Lý do | Hệ quả cần theo dõi |
|---|---|---|---|---|---|
| 2026-09-11 | Chọn US Accidents làm bộ dữ liệu duy nhất | Có phân lớp và luật kết hợp cùng cấp sự cố; `ID` còn trong bảng tiền xử lý | Olist, Airbnb | Có thể tổng hợp hai kỹ thuật trên cùng sự kiện | Chỉ được kết luận trong đúng snapshot đã dùng |
| 2026-09-11 | Đổi cách diễn giải `Severity` thành mức ảnh hưởng giao thông | Mô tả chính thức của bộ dữ liệu định nghĩa Severity theo ảnh hưởng lên traffic flow; snapshot không có trường thương vong | Gọi Severity 3-4 là tai nạn gây thương vong/nghiêm trọng về hậu quả | Tránh sai biến đích và suy diễn vượt dữ liệu | Toàn bộ câu hỏi, insight và khuyến nghị phải dùng thuật ngữ mức ảnh hưởng giao thông |
| 2026-09-11 | Giới hạn phạm vi địa lý ở California và Texas | `State`: CA = 59.903, TX = 20.097, không có bang khác | Gọi kết quả là đại diện US Accidents/toàn nước Mỹ | Phạm vi kết luận phải theo dữ liệu thật | Không khái quát sang bang khác nếu chưa kiểm định ngoài mẫu |
| 2026-09-11 | Dùng `Severity >= 3` làm `is_severe`/`MucDo_Nang` | Code Bài 1/Bài 3 dùng quy tắc này; tỷ lệ nền 17,68% | Đổi ngưỡng sau khi xem kết quả | Bảo toàn khả năng đối chiếu kỹ thuật | Tên trường kỹ thuật không được hiểu là thương vong nặng |
| 2026-09-11 | Tạo split 80/20 theo `ID`, stratify `Severity`, seed 42 | 80.000 ID duy nhất; phân bố bốn lớp lệch | Dùng lại split riêng của Bài 2/Bài 3 | Hai kỹ thuật cần cùng test set | Kiểm tra thêm theo nguồn, bang và thời gian |
| 2026-09-11 | Bắt đầu từ `accidents_preprocessed.csv` | CSV classification đã impute trước split và bỏ `ID` | Dùng trực tiếp output classification/transaction cũ | Giữ khóa sự kiện và loại rò rỉ imputation | Metric mới không cần khớp kết quả bài tập |
| 2026-09-11 | Loại `Distance(mi)` khỏi feature chính | Biến mô tả chiều dài đoạn đường bị ảnh hưởng, có thể chỉ biết đầy đủ sau sự cố | Giữ biến để tăng metric | Mục tiêu là nhận diện sớm bằng thông tin có thể có tại thời điểm ban đầu | Metric có thể giảm nhưng thiết kế phù hợp câu hỏi hơn |
| 2026-09-11 | Giữ 7.236 thời điểm thiếu là `Unknown` | `Start_Time` và `Hour_of_Day` cùng thiếu ở 7.236 dòng trong snapshot; giá trị cuối tuần cũ có thể bị gán ngầm là False | Xem toàn bộ dòng thiếu là ngày thường/ban đêm | Tránh tạo item sai | Chưa thể phục hồi giờ/ngày nếu thiếu raw snapshot |
| 2026-09-11 | Dùng mô hình nhị phân trực tiếp làm model chính | Luật có consequent nhị phân `MucDo_Nang`; cần cùng mục tiêu để đối chiếu score | Cộng xác suất từ mô hình bốn lớp làm score chính | Tránh so hai kỹ thuật khác biến đích; giữ bốn lớp làm nền | Báo cả metric nhị phân và bảng bốn lớp, không trộn hai mục tiêu |
| 2026-09-11 | Chọn model nhị phân theo average precision CV trên train | Nhóm dương chỉ 17,68%; AP đánh giá thứ hạng precision-recall tốt hơn accuracy | Chọn theo accuracy, test score hoặc F1 tại một ngưỡng duy nhất | Không dùng test chọn mô hình và chú trọng lớp mục tiêu | Báo thêm precision, recall, F1, balanced accuracy, ROC AUC và Brier |
| 2026-09-11 | Chọn luật trên train với support 1%, confidence tối thiểu 20%, lift tối thiểu 1,2 | Support 3% chỉ còn 10 luật hướng `MucDo_Nang`; 1% cho 55 luật để lọc | Chọn luật sau khi nhìn test | Giữ test làm đánh giá độ bền | Luật cuối phải có số mẫu tuyệt đối, CI và kết quả theo tầng |
| 2026-09-11 | Hiệu chỉnh xác suất model bằng sigmoid CV trên train | Score từ cây cần calibration trước khi gọi là risk | Dùng trực tiếp xác suất thô | Đối chiếu score giữa nhóm luật và nhóm còn lại nhất quán hơn | Score vẫn chưa phải xác suất vận hành đã xác nhận ngoài mẫu |
| 2026-09-11 | Thay nhãn “xác nhận” bằng “đồng hướng” | Model và rule cùng học từ một dataset, không phải hai nguồn kiểm chứng độc lập | Gọi model xác nhận rule | Tránh phóng đại sức mạnh bằng chứng | Mọi bảng/report dùng đúng nhãn quan hệ mới |
| 2026-09-11 | Kiểm tra luật theo Source và State trước khi chọn insight | Severe rate khác mạnh: Source1 4,36%, Source2/3 khoảng 35,3%; R04 và R08 đảo chiều ở Source2 | Chỉ so nhóm luật với toàn bộ test | Giảm nguy cơ Simpson/thiên lệch nguồn | R04 và R08 không dùng làm phát hiện chính; audit năm dùng để mô tả thêm do nhiều tầng có mẫu nhỏ |
| 2026-09-11 | Chọn Balanced Random Forest làm kỹ thuật nâng cao dự kiến | Baseline nhị phân còn bỏ sót 919/2.829 mẫu dương và tạo 4.444 false positive | Chọn thuật toán phức tạp chỉ vì kỳ vọng metric cao | Kỹ thuật gắn với hạn chế mất cân bằng có bằng chứng | Phải dùng cùng split/features/AP CV và kết luận trung thực về đánh đổi |
| 2026-09-14 | Khóa Q1-Q3 theo đúng biến đích và pipeline đã triển khai | Transaction có thời gian, thời tiết, ánh sáng, hạ tầng; model dự báo `Severity 3-4`; bảng rules-model có audit Source/State | Q1 chứa tầm nhìn chưa được rời rạc hóa; Q2 gọi nhãn là thời lượng gián đoạn; Q3 dùng “xác nhận” tuyệt đối | Bảo đảm câu hỏi trả lời được bằng artifact hiện có và không vượt ý nghĩa `Severity` | Dùng nguyên văn ba câu trong data contract ở README, notebook, Phiếu đề xuất và các Mục 1, 3, 5, 8 của báo cáo |
| 2026-09-14 | Bổ sung chẩn đoán bỏ sót và cảnh báo nhầm theo từng nhóm luật | Q2 hỏi cả false negative và false positive; bảng D.6 trước đó chỉ có recall/FN theo rule | Chỉ dùng recall và số false negative để mô tả lỗi | Precision, FP và FPR cho thấy đánh đổi trong từng lát cắt có thể diễn giải; tránh gọi nhóm recall cao là tốt nếu cảnh báo nhầm quá nhiều | Không cộng số lỗi giữa các rule vì antecedent có thể chồng lấp; D.7 dùng các cột này để chọn nhóm cần phân tích sâu |
| 2026-09-14 | Khóa lưới tham số BRF (`n_estimators ∈ {200,400}`, `max_depth ∈ {8,12}`, `min_samples_leaf ∈ {20,50,100}`) và tiêu chí chọn (Average Precision, 5-fold Stratified CV trên train) trước khi chạy test | Baseline đã dùng đúng thiết kế CV/AP này ở D.4; cần giữ nguyên để so sánh công bằng | Mở rộng lưới tham số sau khi thấy kết quả test không như kỳ vọng | Tránh rò rỉ thông tin test vào lựa chọn mô hình, đúng quy tắc B.3.1 | Nếu chạy lại notebook, tham số tốt nhất phải sinh lại từ đúng lưới này |
| 2026-09-14 | Chọn tham số BRF cuối: `n_estimators=200`, `max_depth=12`, `min_samples_leaf=20` (AP CV = 0,3550); đánh giá trên test đúng một lần | Kết quả GridSearchCV trên train; test 16.000 dòng chỉ predict một lần sau khi khóa tham số | Chạy lại nhiều lần trên test để chọn tham số tốt hơn | Bảo đảm test vẫn là đánh giá độc lập, không dùng để chọn mô hình | Không đổi tham số này chỉ vì số liệu test không như kỳ vọng |
| 2026-09-14 | Giữ cùng phương pháp hiệu chỉnh xác suất (`CalibratedClassifierCV`, sigmoid, `cv=5`) cho BRF như baseline | Baseline (D.4) đã hiệu chỉnh bằng sigmoid CV trên train | Dùng trực tiếp xác suất thô của BRF hoặc đổi sang isotonic | Giữ `risk_severe` của hai mô hình cùng thang đo để đối chiếu công bằng trong bảng before/after | Khi dùng `risk_severe` của BRF để đối chiếu với rule ở Mục 3 và Mục 5, phải nêu rõ đây là xác suất đã hiệu chỉnh, không phải xác suất thô |
| 2026-09-14 | Dùng hồ sơ hạ tầng (`Traffic_Signal`, `Crossing`, `Stop`, `Junction`) và `Source` để mô tả nhóm bị bỏ sót, thay vì chỉ báo một con số FN tổng | FN có tỷ lệ `Traffic_Signal`/`Crossing`/`Stop` cao hơn nhóm nhận diện đúng 3,05–11,14 điểm phần trăm; `Source3` bỏ sót 59,52% nhưng chỉ 126 sự cố dương | Chỉ báo số FN tổng (919→878) mà không mô tả nhóm nào tập trung lỗi | Trả lời đúng vế “bỏ sót ở nhóm điều kiện nào” của Q2; làm nguyên liệu để chọn evidence card về giới hạn ở Mục 5 | Quan sát `Source3` chỉ mang tính thăm dò do cỡ mẫu nhỏ, không dùng làm phát hiện chính nếu chưa kiểm tra thêm |
| 2026-09-14 | Kết luận Balanced Random Forest là cải thiện biên (marginal), có đánh đổi rõ ràng, không dùng làm bằng chứng “kỹ thuật nâng cao vượt trội” | So với baseline: AP 0,3668→0,3722, ROC AUC 0,7312→0,7367, recall 67,52%→68,96% (giảm 41 bỏ sót); nhưng precision 30,06%→29,29%, F1 41,60%→41,11%, Brier 0,1290→0,1301, FP tăng 4.444→4.711 (+267) | Chỉ báo cáo các chỉ số tăng (AP, recall) và bỏ qua các chỉ số giảm | Đúng yêu cầu C4.4 của kế hoạch: “kết luận không giúp vẫn hợp lệ nếu giải thích được vì sao”; số liệu không đủ mạnh để khẳng định vượt trội | Mục 4.4, Mục 5 và Mục 8 của báo cáo không được viết Balanced Random Forest là “giải pháp cải thiện rõ rệt” |
| 2026-09-15 | Chọn bốn evidence cards cuối: R01, R03, đánh đổi BRF và giới hạn R08 | `rules_model_test.csv`; `rules_stratified_robustness.csv`; `model_comparison_before_after.csv` | Chuyển cả 10 luật thành insight độc lập hoặc chỉ chọn luật có lift cao | Bao phủ Q1-Q3, giảm trùng lặp và có một phát hiện bắt buộc về giới hạn | Giữ R01/R03 làm phát hiện tích cực; BRF và R08 thể hiện đánh đổi/giới hạn |
| 2026-09-15 | Không dùng R04/R08 làm phát hiện tích cực hoặc khuyến nghị | R04 và R08 đảo chiều quan sát tại Source2 | Giữ hai luật vì lift mẫu gộp cao | Tránh hiệu ứng gộp che khuất khác biệt theo nguồn | R08 chỉ minh họa thiên lệch nguồn; mức khẳng định của Q1 được giảm phù hợp |
| 2026-09-15 | Giữ kết quả BRF của Phúc làm evidence card về đánh đổi, không xem là mô hình vượt trội | FN giảm 919→878 nhưng FP tăng 4.444→4.711; precision, F1, balanced accuracy và Brier đều xấu hơn | Chọn BRF chỉ vì AP và recall tăng | Báo cáo đầy đủ đánh đổi và ghi đúng nguồn kết quả kỹ thuật của Phúc | Không đề xuất thay baseline hoặc tự động hóa quyết định chỉ từ BRF |
